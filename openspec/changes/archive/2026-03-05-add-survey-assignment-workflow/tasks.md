## 0. 测试先行约束落地（BDD/TDD Gate）

- [x] 0.1 基于 `proposal + specs` 先编写完整 Gherkin 场景（覆盖模板、任务、提交、进度、结果、权限、审计）。
- [x] 0.2 对 Gherkin 场景进行评审并冻结为开发前置输入（未完成不得进入业务编码）。
- [x] 0.3 调研并整理当前后端各层（domain/application/presentation/infrastructure）单元测试风格与断言约定。
- [x] 0.4 形成本变更 TDD 执行规范（Red-Green-Refactor、命名、fixture、mock 边界、覆盖率目标）。

## 1. 领域与数据建模（TDD）

- [x] 1.1 先写领域层失败测试，再实现模板、模板版本、填写任务、任务参与人、提交记录及相关状态/题型实体和值对象。
- [x] 1.2 先写应用层失败测试，再定义模板 CRUD+发布、任务生命周期、提交覆盖写入、结果查询所需仓储/网关端口。
- [x] 1.3 为问卷模块全部数据表创建数据库迁移和 ORM 映射，并建立必要索引。

## 2. 后端应用层与 HTTP API（TDD）

- [x] 2.1 先写失败测试，再实现模板相关命令/查询与 HTTP 控制器：创建、列表、详情、更新、发布。
- [x] 2.2 先写失败测试，再实现填写任务相关命令/查询与 HTTP 控制器：创建、列表、详情、关闭（含进度字段）。
- [x] 2.3 先写失败测试，再实现用户作答 API：获取我的提交（`my-submission`）与提交我的答案（覆盖语义）。
- [x] 2.4 先写失败测试，再实现结果 API：任务提交明细与任务汇总聚合。
- [x] 2.5 先写失败测试，再实现结果可见性的权限校验与无权限响应。
- [x] 2.6 先写失败测试，再实现任务自动收集完毕与手动结束状态流转规则。
- [x] 2.7 先写失败测试，再实现截止时间与已完成状态下的提交拦截逻辑。

## 3. 审计、安全与可观测性

- [x] 3.1 增加原始答卷访问审计事件的数据模型与持久化。
- [x] 3.2 在查看详细结果接口中写入审计事件（操作者、任务标识等）。
- [x] 3.3 为授权审计人员提供审计记录查询/导出能力。

## 4. 前端产品化改造（E2E 驱动）

- [x] 4.1 用生产版问卷模块入口与路由布局替换当前问卷 demo 页面。
- [x] 4.2 实现模板管理 UI 流程（列表/创建/编辑/发布）。
- [x] 4.3 实现填写任务管理 UI 流程（创建/列表/详情/关闭/进度）。
- [x] 4.4 实现被分发用户填写页面，支持题型渲染与覆盖提交交互。
- [x] 4.5 实现结果查看页面（单用户明细 + 聚合汇总）。
- [x] 4.6 接入生成的 API Client，并补充问卷相关 MSW Mock。

## 5. 自动化验收测试（BDD Stage: HTTP + UI）

- [x] 5.1 增加后端单元测试：版本冻结、状态流转、重复提交覆盖、截止拦截。
- [x] 5.2 增加后端集成测试：持久化与聚合查询行为。
- [x] 5.3 将 Gherkin 场景落地为 HTTP-stage Behave 自动化：覆盖控制器编排、响应契约与关键异常流。
- [x] 5.4 增加 UI-stage Behave 自动化（step 内部使用 Playwright）：覆盖模板发布、任务分发、用户填写、进度查看、结果查看。
- [x] 5.5 验证验收场景：模板冻结、进度 `10/12` 与 `12/12`、重复提交“最后一次生效”、截止拦截、权限拒绝、手动结束。
- [x] 5.6 将 Behave（`--stage http` + `--stage ui`）纳入 CI 质量门禁（未通过不可合并）。
